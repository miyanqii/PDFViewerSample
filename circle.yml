general:
  branches:
    only:
      - master
  artifacts:
    - "app/build/outputs/apk/app-debug.apk"
machine:
  java:
    version: oepnjdk8
  environment:
    ANDROID_HOME: /usr/local/android-sdk-linux
test:
  pre:
    - echo "-----start test-----"
  override:
    - echo "Nothing to do here"
  post:
    - echo "-----end test-----"